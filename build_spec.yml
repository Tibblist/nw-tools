version: 0.1
phases:
  install:
    runtime-versions:
      nodejs: 16.x
  pre_build:
    commands:
      - echo Installing source NPM dependencies...
      - cd web-app && yarn install
  build:
    commands:
      - echo Build started on `date`
      - cd web-app && ls && yarn run build
  post_build:
    commands:
      #- aws s3 rm --recursive "s3://beg-luck-dev/"
      # copy the contents of /build to S3
      - aws s3 cp --recursive --acl public-read ./web-app/build s3://$DeployBucket/
      # set the cache-control headers for service-worker.js to prevent
      # browser caching
      # set the cache-control headers for index.html to prevent
      # browser caching
      - >
        aws s3 cp --acl public-read 
        --cache-control="max-age=0, no-cache, no-store, must-revalidate" 
        ./web-app/build/index.html s3://$DeployBucket/
      # invalidate the CloudFront cache for index.html and service-worker.js
      # to force CloudFront to update its edge locations with the new versions
      - >
        aws cloudfront create-invalidation --distribution-id $Distribution 
        --paths /index.html /service-worker.js
artifacts:
  files:
    - "**/*"
  base-directory: web-app/build
